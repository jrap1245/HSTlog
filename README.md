# HSTlog

is a lightweight logging helper for MCU/Arduino‑style projects that keeps a circular buffer of LogEntrys, 
lets you set per‑output log levels and destinations (serial, MQTT, display, …), 
and exposes log/print/println/printf methods plus configurable callbacks and time‑tracking for timestamps.

