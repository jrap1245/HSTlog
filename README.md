# HSTlog

Is a lightweight logging helper for MCU/Arduino‑style projects that keeps a circular buffer of LogEntries

 - lets you set per‑output log levels and destinations (serial, MQTT, display, …)
 - exposes log/print/println/printf methods plus configurable callbacks and time‑tracking for timestamps.

